# Runtime data

`server.js` creates `orbitnest-db.json` in this folder when the app runs. That file stores demo users, sessions, saved itineraries, custom planners, and guest usage counters for the local/full-stack demo and is intentionally ignored by Git.
